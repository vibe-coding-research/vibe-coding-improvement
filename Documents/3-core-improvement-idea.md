# 개선 아이디어 핵심 제안
> AI 생성 코드의 계보 기반 신뢰 추적 시스템 — 바이브 코딩 AI 개선 아이디어 공모전

---

## 핵심 아이디어: 코드의 출생 증명서 — 계보 기반 신뢰 추적 시스템

바이브 코딩이 만들어내는 근본 문제는 코드의 맥락이 사라진다는 것이다.
핵심 아이디어는 단순하다.

> **코드가 태어나는 순간부터 수정되고 전파되는 전 과정을, 추적 가능한 형태로 기록하고 신뢰의 근거로 삼는다.**

---

## 아이디어 전체 요약

| 영역 | 핵심 아이디어 | 해결하는 문제 | 핵심 기술 |
|---|---|---|---|
| **기술** | 생성 맥락 서명 (GCA) | AI 코드의 출처 불명 | AST + 암호학적 서명 |
| **프로세스** | 세션 단위 계보 추적 | 프롬프트-코드 인과관계 상실 | 세션 그래프 모델링 |
| **운영** | 위험 전파 경로 분석 | 취약점의 조용한 확산 | DAG 기반 위험 전파 |
| **검증** | 설명 가능한 신뢰 점수 | 실행 성공 = 품질 보증 착각 | 다차원 신뢰 스코어링 |

---

## 1) 기술: 생성 맥락 서명으로 코드의 출생 증명서를 발급한다

AI가 코드를 생성하는 순간, 해당 코드 블록에 **생성 맥락 서명(Generation Context Attestation, GCA)** 을 부여한다. 어떤 모델이, 어떤 프롬프트 의도로, 언제 만들었는지를 암호학적으로 코드에 결합한다.

이는 AI 생성 코드를 사후에 **감지**하려는 기존 접근과 근본적으로 다르다. **감지가 아닌 기록이다.**

### GCA 메타데이터 구조

| 메타데이터 필드 | 설명 | 예시 |
|---|---|---|
| `model_id` | 생성에 사용된 AI 모델 및 버전 | claude-sonnet-4-6 |
| `prompt_hash` | 프롬프트의 SHA-256 해시 (내용 비공개 가능) | a3f9c2... |
| `generation_timestamp` | 코드 생성 시각 (UTC) | 2025-06-01T09:32:11Z |
| `session_id` | 바이브 코딩 세션 식별자 | vibe-sess-00421 |
| `parent_block_id` | 수정 전 원본 블록 ID (계보 연결) | block-0192 |
| `attestation_sig` | 위 필드 전체의 HMAC-SHA256 서명 | 7e2b91... |

### 기존 도구와의 차이

| 비교 항목 | 기존 정적 분석 / Git | 본 시스템 |
|---|---|---|
| 분석 관점 | Snapshot (현재 상태) | Lineage (생애 주기) |
| AI 코드 식별 | 불가능 | GCA 서명으로 생성 시점부터 기록 |
| 코드 변경 추적 | 파일 단위 diff | 블록 단위 계보 그래프 |
| 출처 정보 | 없음 | 모델명, 프롬프트 해시, 생성 시각 |

코드가 리팩토링되거나 수정되더라도 AST 기반 구조 분석으로 원본 블록과의 연결을 유지하고, Git 커밋 이력과 결합해 코드 한 줄의 계보를 처음부터 끝까지 그래프로 구성한다.

---

## 2) 프로세스: 바이브 코딩 세션을 추적 단위로 만든다

현재 바이브 코딩에는 **세션의 개념이 없다.** 어떤 프롬프트로 어떤 코드가 나왔는지 기록이 남지 않는다.

개발자가 AI와 대화하는 하나의 세션을 하나의 추적 단위로 정의하고, 그 세션에서 생성된 코드 블록들이 이후 어떻게 수정되고 전파되었는지를 세션 단위 계보 그래프로 시각화한다.

### 세션 단위 추적 정보

| 세션 정보 | 기록 내용 | 활용 방식 |
|---|---|---|
| `session_id` | 바이브 코딩 세션 식별자 | 코드 블록과 세션 연결 |
| `prompt_hash` | 프롬프트의 SHA-256 해시 | 생성 의도 역추적 |
| 생성 시각 | UTC 타임스탬프 | 계보 시계열 구성 |
| 파생 블록 목록 | 세션에서 나온 코드 블록 ID | 전파 경로 시작점 |

3주 후에 버그가 발생했을 때 **"이 코드는 5월 20일 인증 기능 구현 세션에서 시작되었다"** 는 것을 즉시 파악할 수 있게 된다.

---

## 3) 운영: 위험 전파 경로를 자동으로 분석한다

취약점은 혼자 존재하지 않는다. 특정 코드 블록에서 발견된 취약점은 그 블록을 의존하는 다른 블록들로 전파된다. 계보 그래프를 기반으로 위험 전파 경로를 자동으로 추적하고, 영향을 받는 블록들에 전파 위험 점수를 부여한다.

### 위험 전파 점수 감쇠

| 전파 단계 | 위험 점수 계산 방식 | 예시 수치 |
|---|---|---|
| 원본 블록 (0단계) | 취약점 심각도 그대로 반영 | 1.0 |
| 직접 의존 블록 (1단계) | 원본 × 0.7 | 0.70 |
| 간접 의존 블록 (2단계) | 원본 × 0.7² | 0.49 |
| 3단계 이상 | 원본 × 0.7ⁿ | 0.34 이하 |

더 나아가 동일한 AI 모델 버전이 반복적으로 생성하는 취약한 코드 패턴을 클러스터링하여 **모델 수준의 위험 프로파일**을 구성한다.

기존 보안 도구가 *"지금 이 파일에 취약점이 있다"* 고 말한다면,
이 시스템은 **"이 패턴을 생성한 모델이 만든 코드 전체가 위험하다"** 고 말할 수 있다.

---

## 4) 검증: 신뢰 점수를 숫자가 아닌 맥락으로 제공한다

코드 블록마다 신뢰 점수를 산출하되, 숫자 하나로 끝내지 않는다. 네 가지 요소를 결합해 **설명 가능한 신뢰 컨텍스트**를 함께 제공한다.

### 신뢰 점수 구성 요소

| 신뢰 요소 | 의미 | 가중치 |
|---|---|---|
| GCA 서명 유효성 | 생성 맥락 서명이 존재하고 위변조되지 않았는가 | 25% |
| 모델 신뢰 등급 | 해당 모델 버전의 취약점 발생 이력 | 25% |
| 사람 검토 이력 | 검토자 수, 검토 시점, 검토 깊이 | 30% |
| 전파 위험도 | 의존 블록으로부터 전파된 위험 수치 | 20% |

### 신뢰 등급 분류

| 등급 | 점수 | 의미 | 권장 조치 |
|---|---|---|---|
| **TRUSTED** | 0.80 이상 | 검증된 코드 | 정기 재검토 권장 |
| **REVIEWED** | 0.60 ~ 0.79 | 검토됐으나 완전하지 않음 | 배포 전 재확인 |
| **UNVERIFIED** | 0.40 ~ 0.59 | 검토 이력 부족 | 수동 검토 필수 |
| **RISKY** | 0.39 이하 | 출처 불명 또는 위험 전파 | 즉시 격리 및 검토 |

### 설명 가능한 컨텍스트 레포트 예시

```
블록 ID: block-0421  |  신뢰 점수: 0.43 (UNVERIFIED)

[확인] 생성 맥락 서명 확인됨 (claude-sonnet-4-6 / 2025-05-20)
[경고] 동일 모델이 생성한 유사 패턴에서 SQL Injection 취약점 발견 이력 3건
[경고] 사람 검토 이력 없음 (생성 후 14일 경과)
[위험] auth_handler()에 의해 직접 호출됨 (의존 깊이: 1)

권장 조치: 보안 검토 후 배포 결정 필요
```

개발자는 IDE에서 검토 이력이 없는 AI 생성 블록을 즉시 식별하고, 배포 전 어떤 코드를 우선적으로 검토해야 하는지 판단할 수 있다. **실행 성공 여부가 아닌 추적 가능한 근거가 신뢰의 기준이 된다.**
