# 적용 시나리오 및 기대 효과
> AI 생성 코드의 계보 기반 신뢰 추적 시스템 — 바이브 코딩 AI 개선 아이디어 공모전

---

## 적용 원칙

시스템은 개발자의 기존 워크플로우를 최대한 방해하지 않는 방식으로 설계된다.
새로운 도구를 배우는 것이 아니라, **기존 환경에 자연스럽게 녹아드는 것**이 핵심 원칙이다.

---

## 1) 개발 단계: IDE 플러그인으로 생성 시점부터 추적 시작

개발자가 Cursor, VS Code 등의 IDE에서 AI로 코드를 생성하는 순간, 백그라운드에서 GCA 서명이 자동으로 부여된다. 개발자가 별도로 해야 할 작업은 없다.

IDE 사이드패널에는 현재 파일 내 코드 블록별 신뢰 등급이 색상으로 표시되고, 검토 이력이 없는 AI 생성 블록은 별도로 하이라이팅된다.

| 적용 시점 | 동작 방식 | 개발자 경험 |
|---|---|---|
| AI 코드 생성 직후 | GCA 서명 자동 부여, 블록 ID 생성 | 별도 조작 없음 |
| 코드 편집 시 | AST 비교로 원본 블록과 연결 유지 | 자동 계보 갱신 |
| 파일 저장 시 | 신뢰 점수 실시간 업데이트 | 사이드패널 반영 |
| 코드 리뷰 완료 시 | 검토 이력 기록, 신뢰 점수 상승 | REVIEWED 등급 부여 |

---

## 2) 통합 단계: Git PR 과정에 자동 검증 삽입

개발자가 Pull Request를 생성하면 CI/CD 파이프라인에서 계보 분석이 자동으로 실행된다. **RISKY 또는 UNVERIFIED 등급 블록이 포함된 PR은 병합이 차단되거나 경고가 발생한다.** 팀은 별도의 보안 점검 회의 없이도 위험한 코드가 메인 브랜치에 진입하는 것을 구조적으로 막을 수 있다.

| PR 상태 | 포함된 블록 등급 | 시스템 조치 |
|---|---|---|
| 정상 병합 가능 | TRUSTED / REVIEWED만 포함 | 자동 승인 허용 |
| 경고 후 병합 | UNVERIFIED 블록 포함 | 검토 요청 알림 발송 |
| 병합 차단 | RISKY 블록 포함 | PR 차단 및 원인 리포트 |

---

## 3) 운영 단계: 취약점 발생 시 전파 경로 즉시 분석

프로덕션 환경에서 보안 취약점이나 장애가 발생하면, 계보 그래프를 역방향으로 탐색해 원인 블록을 즉시 특정한다.

현재는 개발자가 수동으로 코드를 뒤지며 원인을 찾는 데 수 시간에서 수 일이 걸린다. 이 시스템은 다음과 같은 분석을 자동으로 제공한다.

> *"이 장애는 6주 전 결제 모듈 세션에서 생성된 블록에서 시작되었고, 현재 17개의 의존 블록에 위험이 전파되어 있다."*

| 장애 대응 단계 | 기존 방식 | 본 시스템 적용 시 |
|---|---|---|
| 원인 블록 특정 | 수동 코드 탐색, 수 시간 소요 | 계보 역추적으로 즉시 특정 |
| 영향 범위 파악 | 팀원 경험에 의존 | 전파 그래프로 자동 산출 |
| 재발 방지 | 개별 패치 후 종료 | 동일 모델 패턴 전체 경고 |

---

## 4) 기대 효과

### 정량적 효과

| 측정 지표 | 개선 전 | 개선 후 (예상) |
|---|---|---|
| 장애 원인 파악 시간 | 평균 4~8시간 | 30분 이내 |
| 미검토 AI 코드 비율 | 측정 불가 | 실시간 가시화 |
| 보안 취약점 사전 차단 | CI/CD 스캐너 수준 | 계보 기반 예측 차단 추가 |
| 신규 입사자 코드 파악 시간 | 몇 주 소요 | 계보 그래프로 대폭 단축 |

### 정성적 효과

**개발 문화 측면**
AI 생성 코드에 대한 책임 소재가 명확해진다. 지금까지 "AI가 짠 코드라서 몰랐다"는 말이 통용되었다면, 이 시스템 도입 이후에는 검토 이력이 없는 코드를 배포한 개발자가 명시적으로 책임을 인식하게 된다. 이는 처벌이 아닌 **구조적 책임 분배**다.

**유지보수 측면**
수년이 지난 후에도 코드의 생성 맥락을 확인할 수 있어, 신규 개발자나 인수인계 상황에서 발생하는 정보 손실이 크게 줄어든다. "무서워서 건드리지 못하는 코드"가 아니라 **"맥락을 알기 때문에 자신 있게 수정할 수 있는 코드"** 가 된다.

**규제 대응 측면**
EU AI Act 등 AI 생성 코드에 대한 책임 규제가 강화되는 흐름 속에서, 생성 맥락 서명 기반의 추적 이력이 컴플라이언스 증거로 직접 활용될 수 있다. 규제를 피하는 것이 아니라 **추적 체계 자체가 규제 대응 인프라**가 된다.
